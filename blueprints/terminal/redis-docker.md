---
ide: terminal
appName: Terminal
description: Redis (Docker + Redis Stack with RedisInsight)
---

Redis Stack includes Modules: RedisJSON, RediSearch, RedisTimeSeries, RedisBloom.
RedisInsight UI: http://localhost:8001. Connect: redis-cli -p 6379

```json
[
  { "action": "shell", "cmd": "mkdir -p {name}", "cwd": "{outputDir}" }
]
```
