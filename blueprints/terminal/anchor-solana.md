---
ide: terminal
appName: Terminal
description: Solana Smart Contract (Rust + Anchor Framework)
---

anchor init generates a Solana program with Rust backend + TypeScript test suite.
Requires: Rust, Solana CLI, Anchor CLI. Build: anchor build. Test: anchor test.

```json
[
  { "action": "shell", "cmd": "anchor init {name}", "cwd": "{outputDir}", "timeout": 60000 }
]
```
