---
ide: terminal
appName: Terminal
description: Next.js React App (TypeScript + Tailwind + App Router)
---

create-next-app scaffolds a full Next.js project with TypeScript, Tailwind CSS, and App Router.
--yes skips interactive prompts, accepting all defaults.

```json
[
  { "action": "shell", "cmd": "npx create-next-app@latest {name} --typescript --tailwind --app --eslint --yes", "cwd": "{outputDir}", "timeout": 90000 }
]
```
