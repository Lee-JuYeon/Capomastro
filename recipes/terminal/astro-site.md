---
ide: terminal
appName: Terminal
description: Astro Static Site (TypeScript)
---

create astro scaffolds a new Astro project. --template minimal --install --no-git for headless mode.

```json
[
  { "action": "shell", "cmd": "npx create-astro@latest {name} --template minimal --typescript strict --install --no-git", "cwd": "{outputDir}", "timeout": 90000 }
]
```
